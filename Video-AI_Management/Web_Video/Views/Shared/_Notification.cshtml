<div class="modal fade" id="notificationModal" tabindex="-1" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div id="notificationModalContent" class="modal-content">
        </div>
    </div>
</div>

<!-- Dùng cú pháp Bootstrap 4/jQuery -->
<button type="button" id="triggerModalButton" class="btn btn-primary" data-toggle="modal" data-target="#notificationModal" style="display:none;">
    Launch demo modal
</button>


@if (TempData["notification"] != null)
{
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script>
        $(function () {
            const notificationModel = '@TempData["notification"]'.split(";");
            let isSuccess = (notificationModel[0] === 'true');
            let title = notificationModel[1];
            let message = notificationModel[2];
            let type = notificationModel[3];

            displayNotification(isSuccess, title, message, type);
        });
    </script>
}

<script>
    function displayNotification(isSuccess, title, message, type = 'modal') {
        $('#notificationModalContent').empty();

        if (type == 'modal') {
            var modalContentInnerHtml =
                `
                <div class="modal-header ${isSuccess === true ? 'bg-success' : 'bg-danger'}">
                    <h5 class="modal-title" id="modalLabel">${title}</h5>
                    <button type="button" class="btn-close" data-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    ${message}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">OK</button>
                </div>`;

            $("#notificationModalContent").append(modalContentInnerHtml);
            $("#triggerModalButton").trigger('click');
            // === THÊM: Dọn dẹp DOM khi modal này đóng ===
            $('#notificationModal').on('hidden.bs.modal', function () {
                $('.modal-backdrop').remove();
                $('body').removeClass('modal-open').css({
                    overflow: '',
                    paddingRight: ''
                });
                // Xóa handler để tránh chạy nhiều lần
                $(this).off('hidden.bs.modal');
            });
        } else {
            toastr.options = {
                positionClass: "toast-bottom-right"
            };

            if (isSuccess) {
                toastr.success(message);
            } else {
                toastr.error(message);
            }
        }
    }
</script>

